{
  "name": "Mars Control Station (Linux)",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "../.."
  },
  "remoteUser": "mars",
  "updateRemoteUserUID": true,
  "initializeCommand": "docker network inspect mars-dev >/dev/null 2>&1 || docker network create mars-dev",
  "runArgs": ["--network=mars-dev", "--network-alias=mars-control-station"],
  "mounts": [
    "source=${localWorkspaceFolder}/${localEnv:MARS_JETSON_REL:../mars-jetson},target=/workspaces/mars-jetson,type=bind"
  ],
  "containerEnv": {
    "HOST": "0.0.0.0",
    "JETSON_IP": "mars-jetson",
    "RMW_IMPLEMENTATION": "rmw_zenoh_cpp",
    "ROS_AUTOMATIC_DISCOVERY_RANGE": "LOCALHOST",
    "ROS_DOMAIN_ID": "42",
    "ZENOH_CONFIG_OVERRIDE": "mode=\"client\";connect/endpoints=[\"tcp/mars-jetson:7447\"]"
  },
  "forwardPorts": [3000, 3001, 6767, 6969, 9090],
  "portsAttributes": {
    "3000": { "label": "React UI", "onAutoForward": "openBrowser" },
    "3001": { "label": "Application WebSocket" },
    "6767": { "label": "Camera 1 signaling" },
    "6969": { "label": "Camera 2 signaling" },
    "9090": { "label": "rosbridge websocket" }
  },
  "postCreateCommand": "LINE='source ${containerWorkspaceFolder}/ros/ros-env.sh'; grep -qxF \"$LINE\" ~/.bashrc || echo \"$LINE\" >> ~/.bashrc; rosdep update --rosdistro jazzy && MARS_JETSON_SRC=/workspaces/mars-jetson ./ros/setup-ros-ws.sh && npm --prefix server ci && npm --prefix react-app ci",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "eamodio.gitlens",
        "ms-iot.vscode-ros"
      ]
    }
  }
}
